services:
  nginx:
    build: .
    container_name: sipi-frontend-nginx
    ports:
      - "8001:80"
    volumes:
      - ./dist:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped